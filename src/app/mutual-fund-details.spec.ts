import { MutualFundDetails } from './mutual-fund-details';

describe('MutualFundDetails', () => {
  it('should create an instance', () => {
    expect(new MutualFundDetails()).toBeTruthy();
  });
});
